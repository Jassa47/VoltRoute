package com.example.voltroute.ui.theme

import androidx.compose.ui.graphics.Color

// Primary - Cyan/Teal from logo circle
val PrimaryCyan = Color(0xFF00D9FF)
val PrimaryCyanDark = Color(0xFF00A5CC)

// Secondary - Gold/Orange from VR letters
val SecondaryGold = Color(0xFFFFA726)
val SecondaryGoldDark = Color(0xFFFF8A50)

// Background colors
val BackgroundDark = Color(0xFF0A1929)  // Dark blue from logo
val BackgroundLight = Color(0xFFFAFAFA)

// Surface colors
val SurfaceDark = Color(0xFF1A2332)     // Slightly lighter than background
val SurfaceLight = Color(0xFFFFFFFF)

// Legacy support (if needed elsewhere in codebase)
val Primary = PrimaryCyan
val PrimaryDark = PrimaryCyanDark
val Secondary = SecondaryGold
val SecondaryDark = SecondaryGoldDark
